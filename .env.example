# Caddy listens inside the container only; the tunnel reaches it on this port.
# Leave it as :80 — TLS is handled by Cloudflare, not here.
SITE_ADDRESS=:80

# Cloudflare Zero Trust -> Networks -> Tunnels -> your tunnel -> install token.
# Route the tunnel's public hostname to  http://web:80
TUNNEL_TOKEN=
